apiVersion: v1
kind: Service
metadata:
  name: dynamodb-local
  namespace: url-shortener
  labels:
    app: dynamodb-local
spec:
  clusterIP: None
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: dynamodb-local
