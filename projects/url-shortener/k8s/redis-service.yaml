apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: url-shortener
  labels:
    app: redis
spec:
  clusterIP: None  # Headless service for StatefulSet (though we're using Deployment here)
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app: redis
