apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: url-shortener
  labels:
    app: zookeeper
spec:
  clusterIP: None
  ports:
    - port: 2181
      targetPort: 2181
      protocol: TCP
  selector:
    app: zookeeper
